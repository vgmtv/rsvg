image: node:latest

cache:
  paths:
    - node_modules/

before_script:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > .npmrc
  - yarn install

build:
  stage: build
  script:
    - yarn build

deploy:
  stage: deploy
  script:
    - yarn semantic-release
  only:
    - master

