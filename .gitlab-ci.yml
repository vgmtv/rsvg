cache:
  paths:
    - node_modules/

before_script:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > .npmrc
  - yarn install

build:
  stage: build
  tags:
    - vgm
    - library
  script:
    - yarn build

deploy:
  stage: deploy
  tags:
    - vgm
    - library
  script:
    - yarn semantic-release
  only:
    - master

